---
# - name: Install Git on CentOS
#   dnf:
#     name: git
#     state: present
#   # when: ansible_distribution == 'rhel'

# - name: Install Git on Ubuntu
#   apt:
#     name: git
#     state: present
#   when: ansible_distribution == 'Ubuntu'

- name: 
  set_fact:
    project_directory: "{{ '/home/ubuntu/project' if ansible_distribution == 'Ubuntu' else '/home/ec2-user/project' }}"

- name : Task to clone github repo
  git:
    repo: "{{repository}}"
    dest: "/var/www/"
    clone: yes
   


